/**
 * This is the description of the interface
 *
 * @interface IDecodeAccessTokenInput
 * @property {string} accessToken is used for whatever reason
 */
export interface IDecodeAccessTokenInput {
  accessToken: string
}

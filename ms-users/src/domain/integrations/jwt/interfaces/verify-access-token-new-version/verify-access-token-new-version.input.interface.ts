/**
 * This is the description of the interface
 *
 * @interface IVerifyAccessTokenNewVersionInput
 * @property {string} accessToken is used for whatever reason
 * @property {string} refreshToken is used for whatever reason
 */
export interface IVerifyAccessTokenNewVersionInput {
  accessToken: string
  refreshToken: string
}
